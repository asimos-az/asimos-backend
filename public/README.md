# Public assets

This folder is served directly by the backend (Express `static`) at the root path.

Examples:
- `public/logo.png` -> `https://<your-backend-domain>/logo.png`

Put your production logo file here as `logo.png` so it can be used in email templates.
